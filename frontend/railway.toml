[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
buildTarget = "runtime"

[build.args]
VITE_API_URL = ""

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
numReplicas = 1